# Changelog

## 1.3.0 - 2026-09-19

- 新增桌面快捷方式 `Codex (Proxy)`，从第一个进程开始注入代理，正常启动无需守护器重启。
- 新启动的 Codex 先进入短暂流量验证窗口；已走代理则直接接管，未走代理才执行一次兜底重启。
- 同一代理端点短暂中断后恢复默认不再重启 Codex。
- 卸载时只在目标和参数匹配本安装目录时删除专用快捷方式。

## 1.2.2 - 2026-09-19

- 计划任务改由 Windows 11 `conhost.exe --headless` 托管 PowerShell，不创建 Windows Terminal 窗口。
- 守护器进程由任务计划独立持有，重启 Codex 进程树时不会终止自身。

## 1.2.1 - 2026-09-19

- 计划任务 API 不可用时，同时注册当前用户 Startup 快捷方式和 `Run` 登录项。
- 双启动入口由命名互斥锁去重，避免某个 Windows 启动机制被跳过时守护器未运行。

## 1.2.0 - 2026-09-19

- 使用由计划任务托管的隐藏 VBScript 启动器，避免 Windows Terminal 开机弹出空白窗口。
- 隐藏宿主在守护器异常退出时等待 60 秒自动重试，也可由启动文件夹快捷方式托管。
- 守护器在登录后常驻等待；检测到用户后来启动 Codex 时，自动受控重启一次并注入进程级代理。
- 每次守护器启动时重置 Codex 进程边沿状态，确保重启电脑后首次打开 Codex 仍会被接管。

## 1.1.0 - 2026-09-18

- 增加 CONNECT、TLS 和 WebSocket Upgrade 组合健康检查。
- 为 Codex 进程树同时注入代理环境变量和 `--proxy-server`。
- 增加启动配置指纹，升级代理注入策略时执行一次受控迁移。
- 保留防抖、单实例、故障宽限、重启限速、熔断、日志轮换和安全卸载。
- 加入静态/在线自测与可复现发布包构建脚本。
